"""pbcheck's report renderer.

``render_markdown``, ``render_html`` and ``write_outputs`` are added here by the rendering
package of this same v0.1.0 release (a parallel work package fills this package; this file must
not import anything yet, so that WP0 and that work package can be developed independently).
"""
